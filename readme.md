Basic network intrusion classification model for study project. Based on the CIC-IDS-2017 dataset.

To run, install requirements.txt. Training script: train_debug.py. Evaluation script: test_only.py.

Dataset is downloaded to a ```dataset``` directory. Trained models are saved to a ```model``` directory.